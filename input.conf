# claudefm — mpv keybindings
# These extend mpv's built-in bindings; q / space / m still work as default.

UP          add volume 2
DOWN        add volume -2
+           add volume 2
-           add volume -2
WHEEL_UP    add volume 2
WHEEL_DOWN  add volume -2
